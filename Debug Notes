Errors:

1. Program doesn't compile. Only compiles after I comment out anything to do with AccountManager::searchAccounts. That is the main bottleneck.
   There are also issues with initializing Accounts objects and methods, and the accounts vector in Account Manager.

2. After commenting the above out, the issue goes to std::cin on both this BankingSimulator program and guessNum project.

   - There is some characteristic of that function that is unknown to me, as everytime it triggers it doesn't stop to wait for user input.
     It seems that it is using previous input as the next one, resulting in repeated "invalid" messages from the while loops.

   - This again occurs in the main menu. Pressing 2 (View Account Details) executes, but then for some reason instead of going back to
     the main menu and waiting for user inout to determine next step, it automatically executes case 3 without 3 being inputted.